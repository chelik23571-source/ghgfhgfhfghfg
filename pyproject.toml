[project]
name = "nft-leads-bot"
version = "0.1.0"
description = "Consent-based Telegram NFT lead inbox"
requires-python = ">=3.11"
dependencies = [
  "aiogram>=3.10,<4",
  "telethon>=1.36,<2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
